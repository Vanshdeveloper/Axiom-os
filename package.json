{
  "name": "axiom-os",
  "version": "1.0.0",
  "description": "Axiom OS: The AI-Powered Autonomous Productivity System",
  "main": "src/server.js",
  "author": "Axiom OS Team",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "postinstall": "puppeteer browsers install chrome",
    "setup": "npm install && npx puppeteer browsers install chrome",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@mozilla/readability": "^0.6.0",
    "@notionhq/client": "^2.2.14",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsdom": "^29.0.1",
    "puppeteer": "^24.40.0",
    "telegraf": "^4.15.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "nodemon": "^3.0.3"
  }
}
